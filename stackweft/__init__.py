"""StackWeft — single-agent, fixed-workflow end-to-end full-stack delivery.

Level-based model routing, per-call observability, an agentic tool loop, and
requirement memory. Stdlib-only, so it runs with zero install.
"""

__version__ = "0.0.1"
